# Test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# NOTE: neither pytest nor pytest-asyncio was previously listed anywhere in
# this repo (CODE_REVIEW.md #18) — tests/*.py assumed they were present.
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
